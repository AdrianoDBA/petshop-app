# Backend Configuration
PORT=3001
NODE_ENV=development
# Database (for Sequelize)
DB_DIALECT=sqlite
DB_STORAGE=./petshop.db
# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=1h
# Bcrypt
BCRYPT_SALT_ROUNDS=12
# Cookie
COOKIE_MAX_AGE=24h

# Frontend Configuration (if needed)
VITE_API_BASE_URL=/api